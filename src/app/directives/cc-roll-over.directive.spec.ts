import { CcRollOverDirective } from './cc-roll-over.directive';

describe('CcRollOverDirective', () => {
  it('should create an instance', () => {
    const directive = new CcRollOverDirective();
    expect(directive).toBeTruthy();
  });
});
